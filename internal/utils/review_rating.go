package utils

import (
	// "github.com/k0kubun/pp/v3"
	"github.com/tebeka/selenium"
)

func FetchReviewRatings(webDriver selenium.WebDriver) map[string]string {
	reviewRating := make(map[string]string)
	return reviewRating

}
